In the online store "{{vendor.name}}" an order N{{order.public_id}} was issued for the sum of {{order.total_with_delivery_price | money_with_currency}}

Contact details:
Phone: {{order.phone}}
Email: {{order.email}}

Payment method: {{order.payment_type.title}} {% if order.payment_type.description%} - {{order.payment_type.description}} {% endif%}
Delivery type: {{order.delivery_type.title}}

Delivery City: {{order.city_to_delivery}}
Delivery Address: {{order.address_to_delivery}}

Order comment: {{order.comment}}

Order Details:
{% for item in order.items%}
{{forloop.index}}. {{item.title}} ({{item.price | humanized_money_with_symbol}}, {% if item.selling_by_weight?%} {{item.weight}} {{item.weight_unit}} {% else%} {{item. count}} {{item.quantity_unit}} {% endif %}) = {{item.total_price | humanized_money_with_symbol}} {{item.operator_url}}
{% endfor%}
{% if order.package_good%}
Package: {{order.package_good.title}} ({{order.package_price | humanized_money_with_symbol}})
{% endif%}
{% if order.delivery_price> 0%}
Delivery price: {{order.delivery_price | humanized_money_with_symbol}}
{% endif%}
Total: {{order.total_with_delivery_price | humanized_money_with_symbol}}

Order page: {{order.operator_url}}
--
{{vendor.operator_url}}