Заказ {{ order.public_id }} на сумму {{order.total_with_delivery_price | money_with_currency }} получил статус "{{order.workflow_state.title}}"

Страница управления заказом: {{ order.operator_url }}

--
{{ vendor.operator_url }}
