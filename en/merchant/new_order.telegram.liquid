{{vendor_name}}: completed order N{{order.public_id}} for the sum of {{order.total_with_delivery_price | humanized_money_with_symbol}} {% if order.delivery_type.selfdelivery? %}. Pickup address: {{order.delivery_type.pickup_address}} {% else %}. Delivery type: {{order.delivery_type.title}} {% endif %}