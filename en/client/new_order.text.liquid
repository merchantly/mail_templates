Hello, {{ order.name }}!

In the online store "{{vendor.name}}" issued an order N{{ order.public_id }} to the amount of {{order.total_with_delivery_price | money_with_currency }}

Contact details:
Phone: {{order.phone}}
Email: {{order.email}}

Payment method: {{order.payment_type.title}}{% if order.payment_type.description %} – {{ order.payment_type.description }}{% endif %}

Delivery method: {{order.delivery_type.title}}{% if order.delivery_type.description %}. {{ order.delivery_type.description }}{% endif %}
{% if order.delivery_type.selfdelivery? %}Address ex: {{order.delivery_type.pickup_address}}
{% else %}Delivery City: {{order.city_title}}
Delivery address: {{order.address}}
{% if order.date_from and order.date_till %}Delivery time: from {{ order.date_from }} to {{ order.date_till }}{% endif %}
{% endif %}
Order Details:
{% for item in order.items %} {{forloop.index}}.  {{item.title}} ({{item.price | humanized_money_with_symbol}}, {% if item.selling_by_weight? %} {{item.weight}} {{ item.weight_unit }} {% else %} {{ item.count }} {{item.quantity_unit}} {%endif%}) = {{item.total_price | humanized_money_with_symbol}} {{ item.public_url }}
{% endfor %}
{% if order.package_good %}
Package: {{order.package_good.title}} ({{order.package_price | humanized_money_with_symbol }})
{% endif %}
{% if order.delivery_price > 0 %}
Cost of delivery: {{order.delivery_price | humanized_money_with_symbol}}
{% endif %}
Total: {{order.total_with_delivery_price | humanized_money_with_symbol }}
{% if order.total_vat_out %}
Total VAT: {{ order.total_vat_out | humanized_money_with_symbol }}
{% endif %}

{% if order.delivery_type.mail_comment %}
  {{ order.delivery_type.mail_comment }}
{% endif %}

Track order can be reached at: {{ order.client_order_url }}

{% if order.must_be_paid_online? %}
Link to pay now by {{ order.payment_type.title }} - {{ order.payment_url }}
{% endif %}

--
{{ signature }}
{{ vendor.home_url }}
